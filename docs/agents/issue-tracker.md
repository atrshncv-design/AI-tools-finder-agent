# Local issue tracker

Feature specifications and implementation tickets are stored as markdown
files under `.scratch/<feature-slug>/`. Ticket status values are `ready-for-agent`,
`in progress`, `done`, or `failed`.
