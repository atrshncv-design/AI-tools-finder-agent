# Ralph Progress Log
Started: 2026-06-24
Feature: Приведение проекта в соответствие с ТЗ

## Codebase Patterns
(Patterns discovered during this feature build)

---

## 2026-06-24 08:48 - US-001
- Updated default LM Studio model from zai-org/glm-4.6v-flash to google/gemma-4-e4b across codebase.
- Files changed: app/api/ai/client.ts, .env.example, docker-compose.yml, docs/DEPLOY.md, docs/ADMIN_GUIDE.md, app/README.md, app/docs/DEPLOYMENT.md, app/.env.example.
- Baseline cleanup required to pass quality checks:
  - Removed unused `and` import in app/api/agent/deployAgent.ts.
  - Removed unused `STAGE_ORDER` in app/api/agent/pipeline.ts.
  - Removed unused GigaChat code in app/api/ai/client.ts (gigachat import, client, completion function).
  - Fixed app/api/queries/favorites.ts and app/src/pages/Favorites.tsx to include full News type fields.
  - Fixed lint errors in test-full-cycle.ts, test-gemma-speed.ts, test-gigachat.ts.
  - Created .gitignore.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - Project was not under git; initialized repo to enable Ralph commits.
  - Several pre-existing typecheck/lint errors blocked all stories; fixed as baseline hygiene.
  - Filesystem path for project directory uses decomposed Unicode for "Й" in "НАУЧНЫЙ" — use relative paths in Shell to avoid issues.
---

## 2026-06-24 08:49 - US-002
- Made extractFirstParagraph optional via extractParagraph option in rawChatCompletion/chatCompletion.
- Short summary keeps first-paragraph extraction (3-5 sentences).
- Detailed summary and translation now return full text (extractParagraph: false).
- Files changed: app/api/ai/client.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - rawChatCompletion applies post-processing globally; any text-extraction must be opt-in per call.
  - Translation and detailed summaries need full multi-paragraph output.
---

## 2026-06-24 08:51 - US-003
- Replaced Bing/python-based translation with local LLM via translateArticle() from app/api/ai/client.
- Removed translateViaBing(), translateTitleViaBing(), splitIntoChunks(), and child_process import.
- Title and full text translated in parallel per article; Russian articles still copied as-is.
- Files changed: app/api/agent/translateAgent.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - Batch translation now shares the same LM Studio client config, retries, concurrency, and token limits as summarization.
  - Keep DELAY_BETWEEN_ARTICLES_MS to avoid overwhelming the local LLM server.
---

## 2026-06-24 08:55 - US-004
- Implemented TZ-compliant schedule with three cron jobs:
  - 06:00 daily — full pipeline (runPipeline)
  - 07:00 daily — science-only pipeline (runSciencePipeline)
  - every 4 hours — additional full pipeline
- Added optional scienceOnly parameter to runSummarizeAgent and runTranslateAgent.
- Created runSciencePipeline() in app/api/agent/pipeline.ts.
- Updated app/api/scheduler/index.ts to register and manage three cron tasks.
- Files changed: app/api/agent/summarizeAgent.ts, app/api/agent/translateAgent.ts, app/api/agent/pipeline.ts, app/api/scheduler/index.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - Shared isRunning guard across runPipeline and runSciencePipeline prevents overlap; consider separate guards if pipelines should run concurrently.
  - Science pipeline still parses all sources but only processes isScience=true articles.
---

## 2026-06-24 08:56 - US-005
- Added extractPublishedAt() helper in app/api/agent/parseAgent.ts.
- Checks common selectors: article:published_time, datePublished, pubdate, time[datetime], etc.
- fetchHtmlPage() now returns pubDate for each article.
- Falls back to current date when not found (existing behavior preserved).
- Files changed: app/api/agent/parseAgent.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - HTML date extraction is heuristic-based; different sites use different meta tags.
  - Always validate parsed Date to avoid invalid dates in DB.
---

## 2026-06-24 08:57 - US-006
- Replaced naive Cyrillic/Latin language detection with franc-min library.
- Maps ISO 639-3 codes to 2-letter codes for ru/en/de/es/zh/ja/fr/it/pt/ko/nl/pl/tr/ar/hi.
- Returns "unknown" for texts shorter than 20 chars or when franc returns "und".
- Installed dependency: franc-min.
- Files changed: app/api/agent/parseAgent.ts, app/package.json, app/package-lock.json.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - franc-min is lightweight and pure JS; no native bindings needed.
  - Short snippets (< 20 chars) are unreliable for detection.
---

## 2026-06-24 08:58 - US-007
- Applied maxArticles limit to RSS, Google News, and API source results in parseSource().
- HTML path already sliced; now all source types respect per-source article limit.
- Files changed: app/api/agent/parseAgent.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - parseSource() is the right place to enforce maxArticles uniformly.
---

## 2026-06-24 08:59 - US-008
- Added fetchGoogleNews() and resolveGoogleNewsUrl() in app/api/agent/parseAgent.ts.
- Google News RSS items now follow redirects to get real article URLs.
- Replaced Hacker News (AI) source with Google News AI source in app/api/queries/sources.ts.
- Files changed: app/api/agent/parseAgent.ts, app/api/queries/sources.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - Google News RSS links redirect through news.google.com; resolving them is required for correct originalUrl.
---

## 2026-06-24 09:02 - US-009
- Added createOAuthLoginHandler() in app/api/kimi/auth.ts.
- Generates cryptographically random state via randomBytes(32).toString("base64url").
- Stores state and redirect URI in short-lived httpOnly cookies.
- Updated callback to validate state and use stored redirect URI; removed atob(state).
- Added /api/oauth/login route in app/api/boot.ts.
- Files changed: app/api/kimi/auth.ts, app/contracts/constants.ts, app/api/boot.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - OAuth state must be random and server-side validated; never trust client-provided state or encode sensitive data in it.
---

## 2026-06-24 09:05 - US-010
- Added tokenVersion column to users table (schema + migration 0004_parallel_lightspeed.sql).
- JWT expiry reduced from 1 year to 24 hours; configurable via SESSION_EXPIRY_HOURS env.
- verifySessionToken now checks tokenVersion against DB; mismatched versions reject the token.
- Logout mutation increments tokenVersion, invalidating the token server-side.
- Login/register/OAuth callback include current tokenVersion in JWT payload.
- Files changed: app/db/schema.ts, app/db/migrations/0004_parallel_lightspeed.sql, app/api/kimi/session.ts, app/api/auth-router.ts, app/api/kimi/auth.ts, app/api/queries/users.ts, .env.example, app/.env.example.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - Token versioning is simpler than a blocklist table and provides global logout.
  - Always protect tokenVersion from being overwritten by upsertUser.
---

## 2026-06-24 09:05 - US-011
- Replaced __DOMAIN__ placeholders with $DOMAIN in nginx/conf.d/default.conf.
- deploy.sh envsubst '$DOMAIN' now correctly substitutes the domain.
- Files changed: nginx/conf.d/default.conf.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - envsubst replaces $VAR syntax, not __VAR__ placeholders. Use consistent templating.
---

## 2026-06-24 09:06 - US-012
- app service in docker-compose.yml now uses env_file: .env to forward all environment variables.
- Removed explicit env var duplication and hardcoded host.docker.internal LM_STUDIO_URL.
- DATABASE_URL remains explicitly constructed from POSTGRES_PASSWORD.
- Files changed: docker-compose.yml.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (32 passed).
- **Learnings for future iterations:**
  - env_file is the most maintainable way to pass environment variables in Docker Compose.
---

## 2026-06-24 09:08 - US-013
- Created app/api/agent/parseAgent.test.ts.
- Added tests for containsAiKeywords, detectLanguage, makeDecisions.
- Exported pure functions from app/api/agent/parseAgent.ts to enable unit testing.
- Files changed: app/api/agent/parseAgent.ts, app/api/agent/parseAgent.test.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (43 passed).
- **Learnings for future iterations:**
  - Pure functions in agents should be exported for unit tests.
  - franc-min can misclassify very short snippets; use longer representative text in tests.
---

## 2026-06-24 09:11 - US-014
- Added normalizeUrl(), levenshteinDistance(), titleSimilarity() helpers.
- Integrated semantic deduplication into runParseAgent current batch processing.
- Duplicates detected by normalized URL match or title similarity >= 0.85.
- Added unit tests for normalizeUrl and titleSimilarity.
- Files changed: app/api/agent/parseAgent.ts, app/api/agent/parseAgent.test.ts.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (49 passed).
- **Learnings for future iterations:**
  - Deduplication against current batch prevents same article from multiple sources being inserted twice.
  - Levenshtein similarity is simple and effective for title deduplication.
---

## 2026-06-24 09:12 - US-015
- Git repository was initialized during US-001; .gitignore created.
- Added .github/workflows/ci.yml with typecheck, lint, and test jobs on push/PR to main.
- Files changed: .github/workflows/ci.yml.
- Quality checks: npm run check ✅, npm run lint ✅, npm test ✅ (49 passed).
- **Learnings for future iterations:**
  - CI must match local working directory structure (app/ subdirectory).
---

## 2026-06-24 09:27 - Model swap + local translation
- Changed default LM Studio model from google/gemma-4-12b-qat to google/gemma-4-e4b across configs, docs, and PRD.
- Replaced web/Bing/LLM full-article translation with local ONNX translation via Transformers.js.
  - Added app/api/ai/translateLocal.ts using Xenova/opus-mt-en-ru (default), chunked by sentences.
  - Updated translateAgent.ts to use translateLocal for title and full text; LLM now used only for summarization.
  - Added LOCAL_TRANSLATE_MODEL / LOCAL_TRANSLATE_MAX_CHUNK_CHARS / LOCAL_TRANSLATE_DEVICE env vars.
  - Marked @xenova/transformers and onnxruntime-node as external in esbuild build to keep native bindings working.
  - Increased app container memory limit from 512m to 2g for model inference.
- Verified local translation: "Artificial intelligence is transforming scientific research." -> "Искусственный интеллект преобразует научные исследования."
- Quality: npm run check ✅, npm run lint ✅, npm test ✅ (49 passed), npm run build ✅.
---

## 2026-06-24 09:40 - Switch translation to GigaChat API
- Replaced local Transformers.js/ONNX translation with GigaChat API translation.
  - Added app/api/ai/gigachatTranslate.ts wrapping GigaChat SDK with retries, concurrency limit, and env-based config.
  - Updated translateAgent.ts to call translateWithGigaChat for title and full text.
  - Removed app/api/ai/translateLocal.ts, @xenova/transformers dependency, and onnxruntime-node external build hack.
  - Removed app/test-gigachat.ts hardcoded key file.
  - Added GIGACHAT_API_KEY, GIGACHAT_MODEL, GIGACHAT_TIMEOUT, GIGACHAT_RETRIES, GIGACHAT_CONCURRENCY, GIGACHAT_MAX_TOKENS env vars.
  - Reverted docker-compose app memory limit back to 512m (cloud API, no local model).
- Verified GigaChat translation: "Artificial intelligence is transforming scientific research." -> "Искусственный интеллект трансформирует научное исследование."
- Quality: npm run check ✅, npm run lint ✅, npm test ✅ (49 passed), npm run build ✅.
---

## 2026-06-24 11:15 - Token usage tracking + optional GigaChat summarization + 30-article test cycle
- Added app/api/ai/tokenUsage.ts to accumulate prompt/completion/total tokens for LM Studio and GigaChat.
- Integrated token recording into app/api/ai/client.ts (LM Studio) and app/api/ai/gigachatTranslate.ts (GigaChat).
- Added summarizeWithGigaChat() in app/api/ai/gigachatTranslate.ts; summarizeAgent.ts switches provider via SUMMARY_PROVIDER env (`lmstudio` or `gigachat`).
- Added app/scripts/test-cycle.ts to run full parse → summarize → translate → deploy pipeline on 30 articles and report time + tokens per stage.
- Full test cycle results (GigaChat for both summary and translation, 30 articles):
  - Parse: 6.81s, 0 tokens
  - Summarize: 112.27s, 45,707 GigaChat tokens
  - Translate: 312.48s, 67,033 GigaChat tokens
  - Deploy: 0.13s, 0 tokens
  - Total: 431.69s, 112,740 GigaChat tokens
- Updated env examples and docs with SUMMARY_PROVIDER and GIGACHAT_SUMMARY_MAX_CHARS.
- Quality: npm run check ✅, npm run lint ✅, npm test ✅ (49 passed), npm run build ✅.
---

## 2026-06-24 12:42 - MLX inference experiments and provider switches
- Added TRANSLATION_PROVIDER env switch (`gigachat` / `lmstudio`) in translateAgent.ts.
- Added SUMMARY_PROVIDER env switch (`gigachat` / `lmstudio`) in summarizeAgent.ts with GigaChat summary via marked text.
- Created local MLX inference venv (`venv-mlx`) with `mlx-lm` and tested `meta-llama-3.1-8b-instruct` via `mlx_lm.server`.
- Findings: MLX server prompt processing is fast, but text generation for long outputs (~1024+2048 tokens per article) is still very slow on this machine (~5 min per article). Not viable for 30-article daily cycle.
- gemma-4-E4B MLX 4bit model could not be loaded by `mlx-lm` ("Model type gemma4 not supported").
- Updated .gitignore to exclude venv-mlx.
- Quality: npm run check ✅, npm run lint ✅, npm test ✅ (49 passed), npm run build ✅.
---
